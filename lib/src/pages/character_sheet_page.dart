import 'package:flutter/material.dart';

class CharacterSheetListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CharacterSheet'),
      ),
      body: Center(
        child: Text('Página de inicio de sesión'),
      ),
    );
  }
}
